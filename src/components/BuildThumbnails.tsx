'use client';

interface BuildThumbnailsProps {
  classSlug: string;
  allocation: string;
  size?: number;
}

export default function BuildThumbnails({}: BuildThumbnailsProps) {
  // Talent calculator is WIP; thumbnails hidden until node data is re-sourced.
  return null;
}
