import { item } from './item';
import { article } from './article';
import { newsPost } from './newsPost';
import { page } from './page';
import { siteSettings } from './siteSettings';

export const schemaTypes = [item, article, newsPost, page, siteSettings];
